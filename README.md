# Markdown 预览示例

> 将本文件拖拽到 `index.html` 页面，或点击页面上的文件选择直接打开。

## 标题与列表

- 支持 **粗体**、_斜体_、~~删除线~~
- 自动识别链接：https://example.com
- 引用：
  > 这是一段引用文本。

## 代码高亮

```js
export function greet(name) {
  const msg = `Hello, ${name}!`;
  console.log(msg);
  return msg;
}
```

```python
def fib(n: int) -> int:
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

## 表格

| 语言       | 类型系统     | 生态            |
| ---------- | ------------ | --------------- |
| JavaScript | 动态         | Web/Node.js     |
| TypeScript | 静态（可选） | Web/Node.js     |
| Python     | 动态         | 科学计算/自动化 |

## 图片

![示例图片](https://via.placeholder.com/640x240.png?text=Markdown+Preview)
